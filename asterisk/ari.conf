; VoxGuard Asterisk ARI Configuration
; Reference config for LAN/hackathon demo

[general]
enabled=yes
pretty=no

; The VoxGuard ARI user — used by the Go ARI controller
[voxguard]
type=user
read_only=no
password=REPLACE_WITH_A_LONG_RANDOM_ARI_SECRET
